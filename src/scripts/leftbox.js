class leftBox {
    constructor(ctx, coords, color = "light-grey") {
        this.ctx = ctx;
        this.coors = coords;
        this.color = color;
    }
    clearWithin() {
    
    }
}

export default leftBox;